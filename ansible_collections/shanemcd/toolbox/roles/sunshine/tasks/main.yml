---
# Configuration is managed by chezmoi dotfiles
# This role only ensures the service is enabled and running

- name: Enable and start Sunshine user service
  ansible.builtin.systemd_service:
    name: app-dev.lizardbyte.app.Sunshine
    scope: user
    enabled: true
    state: started
