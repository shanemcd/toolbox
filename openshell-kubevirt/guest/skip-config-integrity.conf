[Service]
# Site Hermes: PVC-backed config.yaml is mutated by onboarding tips; keep
# NemoClaw from fail-closing on hash drift after in-place disk upgrades.
# Requires nemoclaw-start that honors NEMOCLAW_SKIP_HERMES_CONFIG_INTEGRITY.
Environment=NEMOCLAW_SKIP_HERMES_CONFIG_INTEGRITY=1
